MEMORY {
    RAM: start = $0000, size = $7F80, type = rw;
    ROM: start = $8000, size = $7F00, type = ro, fill = yes;
    WOZMON: start = $FF00, size = $00FA, type = ro, fill = yes;
    SYSVEC: start = $FFFA, size = 6, type = ro, fill = yes;
}

SEGMENTS {
    SYSVEC: load = "SYSVEC", type = ro;
    DRIVERS: load = "ROM", type = ro, start = $C000;
    SYS_ROUT: load = "ROM", type = ro;
    WOZMON: load = "WOZMON", type = ro;
}